# Copy to .env (gitignored) or export in your shell. Never commit a real key.
# The value goes on the same line as the name, no quotes needed, nothing on the next line:
#   ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=
